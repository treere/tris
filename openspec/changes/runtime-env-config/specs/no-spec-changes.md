## ADDED Requirements

*(No new capabilities — this change is purely internal infrastructure with no spec-level behavior changes.)*

## MODIFIED Requirements

*(No existing capabilities have requirement changes — admin authentication behavior is identical.)*
